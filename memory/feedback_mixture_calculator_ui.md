# Mixture CalculatorのUI

配合計算表は新しいBlenderウィンドウや3Dビュー領域を作らず、既存Nパネルの
ボタンから横へ開く広いポップオーバーにする。Enabledはチェックボックスのまま
残すが、行選択にはチェックボックスを使わず、行ハイライトを中心に表現する。
表の列幅は固定せず全体幅へ追従させる。行選択とEnabledは狭く、Name入力へ十分な
幅を配分し、数値列は互いに同じ幅とする。
